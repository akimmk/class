import React from "react";

const Header = () => {
  return (
    <div className="header">
      {/* Header content goes here */}
      <h2>Header</h2>
      <p>Search bar, notifications, user menu, etc.</p>
    </div>
  );
};

export default Header;
